version: "3.9"

services:

  backend:
    build: ./backend
    container_name: task-backend
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      MONGO_URI: mongodb+srv://time-manager:Time%40123@cluster0.rlfd3zh.mongodb.net/task_manager?retryWrites=true&w=majority
      JWT_SECRET: devAccessSecret
      JWT_REFRESH_SECRET: devRefreshSecret
      ACCESS_EXPIRE: 15m
      REFRESH_EXPIRE: 7d
      MAIL_USER: mandhalakarthikreddy@gmail.com
      MAIL_PASS: tebg ozwa jetn luis
    volumes:
      - ./backend:/app
    restart: always

  frontend:
    build: ./frontend
    container_name: task-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: always